<!-- RTL-safe README حرفه‌ای برای GitHub -->

<div dir="rtl" align="right">

# 💊🏪 فروشگاه آنلاین دارو — Online Pharmacy Project

این پروژه یک **فروشگاه آنلاین دارو** است که با استفاده از **Python و MySQL** پیاده‌سازی شده و امکان مدیریت محصولات، کاربران و سفارشات را فراهم می‌کند. برنامه با **ماژول‌بندی حرفه‌ای** نوشته شده و برای **Portfolio، پروژه دانشجویی و تمرین مدیریت پایگاه داده** مناسب است.

---

## ✨ قابلیت‌ها

* 🧑‍💻 مدیریت کاربران و ورود/ثبت‌نام
* 🛍️ افزودن، ویرایش و حذف محصولات دارویی
* 🛒 سبد خرید و مدیریت سفارشات
* 🔍 جستجوی محصولات بر اساس نام و دسته‌بندی
* 📄 مشاهده تاریخچه سفارشات
* ⚙️ عملیات پایگاه داده با استفاده از ماژول‌های DDL, DML, DQL
* 💾 ذخیره و بازیابی داده‌ها با **MySQL**

---

## 🛠️ تکنولوژی‌ها و کتابخانه‌ها

* Python
* MySQL
* ماژول‌های داخلی SQL در پایتون
* فایل `config.py` برای مدیریت اتصال به پایگاه داده
* ساختار ماژولار برای مدیریت عملیات SQL

---

## ⚙️ نحوه‌ی عملکرد پروژه

1. اتصال به MySQL از طریق اطلاعات موجود در `config.py`
2. ساخت جداول با اجرای `DDL.py`
3. انجام عملیات درج، حذف و بروزرسانی داده‌ها با `DML.py`
4. بازیابی اطلاعات با `DQL.py`
5. منطق اصلی برنامه در `main.py` اجرا می‌شود و تعامل با کاربر را مدیریت می‌کند

---

## 📂 ساختار پروژه

```text
Online_Pharmacy.Project/
│
├── config.py          # تنظیمات اتصال به پایگاه داده
├── DDL.py             # تعریف جداول
├── DML.py             # عملیات درج/حذف/بروزرسانی
├── DQL.py             # بازیابی داده‌ها
├── main.py            # اجرای برنامه و منطق کاربری
├── text.py            # متن‌ها و رابط متنی
├── requirements.txt   # وابستگی‌ها
└── README.md
```

---

## ▶️ نحوه‌ی اجرا

### 1️⃣ کلون کردن پروژه

```bash
git clone https://github.com/Ghasemi242213/Online_Pharmacy.Project.git
cd Online_Pharmacy.Project
```

### 2️⃣ نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 3️⃣ تنظیم اتصال به MySQL

🔹 فایل `config.py` را باز کرده و اطلاعات دیتابیس (نام، یوزرنیم، پسورد و پورت) را وارد کنید.

### 4️⃣ ساخت جداول

```bash
python DDL.py
```

### 5️⃣ اجرای برنامه

```bash
python main.py
```

📌 حالا می‌توانید فروشگاه را مشاهده و با آن تعامل کنید.

---

## 🚀 موارد استفاده

* تمرین توسعه فروشگاه آنلاین با Python و MySQL
* پروژه دانشجویی و Portfolio
* یادگیری مدیریت پایگاه داده و عملیات SQL
* الگویی برای توسعه فروشگاه‌های دیگر

---

## ⚠️ نکات مهم

* ❌ اطلاعات اتصال به MySQL را در GitHub منتشر نکنید
* 🔐 از رمزهای قوی و امن برای دیتابیس استفاده کنید
* اعتبارسنجی ورودی‌ها و مدیریت خطاها برای نسخه‌های بعدی ضروری است

---

### 📌 پیشنهاد توسعه

* ایجاد رابط وب با Flask یا FastAPI
* افزودن صفحات ورود/ثبت‌نام حرفه‌ای
* مدیریت سبد خرید و پرداخت آنلاین
* API جداگانه برای جستجو و نمایش محصولات

</div>
